apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: cloudops-dashboard

spec:
  selector:
    matchLabels:
      app: cloudops-dashboard

  endpoints:
    - port: http
      path: /metrics
      interval: 30s